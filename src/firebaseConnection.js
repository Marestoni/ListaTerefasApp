import firebase, { initializeApp } from 'firebase/app';
import 'firebase/database'

let firebaseConfig = {
   informações coletadas no banco
  };

  if(!firebase.apps.length){
      firebase.initializeApp(firebaseConfig);
  }

  export default firebase;
